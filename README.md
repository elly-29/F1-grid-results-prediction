# F1-grid-results-prediction

This guide provides step-by-step instructions to create a Conda environment (f1env) with the required dependencies to run the F1 race prediction model using Jupyter Notebook.

